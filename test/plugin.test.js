const test = require('ava')
const log = require('util').debuglog('@caviar/next-roe-define-router-plugin')
const next_roe_define_router_plugin = require('../src')

test('description', t => {
  t.is(true, true)
})
